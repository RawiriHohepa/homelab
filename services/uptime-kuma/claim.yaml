kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: uptime-kuma-claim
  namespace: services
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
