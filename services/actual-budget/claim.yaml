apiVersion: v1
kind: PersistentVolume
metadata:
  name: actual-budget-data-volume
spec:
  capacity:
    storage: 2Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Delete
  storageClassName: longhorn
  csi:
    driver: driver.longhorn.io
    volumeHandle: actual-budget-data-volume

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: actual-budget-data-claim
  namespace: services
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
  volumeName: actual-budget-data-volume
