apiVersion: v1
kind: Service
metadata:
  name: readarr
  namespace: services
spec:
  selector:
    app.kubernetes.io/name: readarr
  ports:
    - protocol: TCP
      port: 8787
      targetPort: 8787
